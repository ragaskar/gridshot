#!/usr/bin/env bash
# Run the gridshot CLI inside the app container — nothing installed on the host.
set -euo pipefail
cd "$(dirname "$(readlink -f "$0")")/.."
# shellcheck source=scripts/lib-compose.sh
source scripts/lib-compose.sh
gridshot_compose_setup
gridshot_compose run --rm cli gridshot "$@"
